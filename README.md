# nbgv-example
Illustrates practical use of Nerdbank.GitVersioning

This repository now includes a minimal .NET class library named `ExampleLib` and a GitHub Actions workflow that restores, builds, packs, and uploads the resulting NuGet package as a build artifact.
